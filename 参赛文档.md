# TCommander 参赛文档

【标签】#学习工作

【标题】【学习工作赛道】TCommander - AI时代的智能终端会话管理器

---

## 1. Demo 简介

**是什么：** TCommander 是一款基于 Electron 的跨平台桌面应用，为 AI 编程时代量身打造的智能终端会话管理器。它能同时管理多个本地终端、SSH 远程会话和 AI 助手进程，自动感知每个会话的运行状态，让开发者一屏掌控所有终端。

**面向谁：** 同时使用多个 AI 编程助手（Claude Code、Codex 等）的开发者、需要管理多台服务器的运维工程师，以及有自动化脚本执行需求的效率型用户。

**主要功能：**

**① 智能状态感知 + Hook 生态集成**
自动检测每个终端的 5 种状态（运行中/需确认/待输入/错误/空闲），通过 MCP Server + HTTP Hook API 让 AI 助手主动推送状态更新。当 Claude Code 等待确认时，卡片自动置顶并出现 Y/N 快捷按钮。

![状态感知](tcommander_demo.html)

**② SSH 远程终端 + 终端实例池**
支持密码/私钥/keyboard-interactive 三种 SSH 认证，safeStorage 加密存储凭证，断线自动重连。独创「终端实例池」架构——一会话一 xterm 实例 + DOM 转移，卡片与全屏共享完整 buffer，不可见时零 CPU 开销。

**③ 卡片即终端（Card-as-Live-Terminal）**
每张卡片直接就是一个可交互的 xterm 终端，支持实时输入和输出预览。双击进入全屏只是 DOM 转移，不销毁不重建，状态零丢失。配合 8+ 终端主题和智能排序（错误 > 需确认 > 待输入 自动置顶），实现真正的"一屏监控，一键响应"。

---

## 2. Demo 创作思路

**灵感来源：**

在重度使用 Claude Code 和 Codex 进行 AI 辅助编程的过程中，我发现一个核心痛点：同时跑 3-4 个 AI 助手实例时，必须不停切换窗口去检查每个终端——某个 AI 是否在等我确认？是否报错了？是否已经完成？大量时间消耗在"来回看"上，严重打断心流。

更头疼的是，传统的终端管理器（iTerm2、Windows Terminal）都是"标签页"模式，一次只能看一个终端。用 tmux 分屏？屏幕太小看不清。开多个窗口？Alt+Tab 切到崩溃。

**想解决的问题：**

- **多终端监控盲区**：同时运行多个 AI 助手/SSH 会话时，无法一屏掌握所有状态，容易遗漏关键等待项
- **确认操作繁琐**：AI 助手频繁请求确认（Allow changes? y/n），每次都要切窗口、找到对应终端、手动输入
- **环境恢复困难**：重启或中断后，需要重新打开所有终端、SSH 连接、恢复工作目录和命令历史
- **远程管理割裂**：本地终端和远程 SSH 会话需要不同工具管理，切换成本高

**为什么做这个方向：**

我的判断是：AI 编程助手正在从"单实例使用"走向"多实例并行"——开发者会同时让多个 Claude Code 实例处理不同模块。这催生了一个新的刚需场景：**多 AI 终端的统一监控和快速响应**。现有的终端工具都没有为这个场景做设计。

取舍上，我放弃了"网页版/SaaS"方向，选择 Electron 桌面应用，因为终端管理需要深度系统能力（PTY、SSH、本地文件、safeStorage 加密），桌面端能提供最佳性能和安全性。同时，通过 Hook 生态（MCP/HTTP/CLI）保持与 AI 助手的开放集成能力，不锁定单一生态。

---

## 3. Demo 体验地址

本项目提供交互式 HTML 演示文件（Zip 打包上传）：

- **`tcommander_demo.html`** — 产品演示页面，包含完整的应用界面模拟（6 种状态的会话卡片）、演示逻辑（4 步工作流）、Hook 集成配置示例和终端主题展示
- **`tcommander_presentation.html`** — 创意介绍页面，包含问题分析、核心功能、Hook 生态架构图、技术架构详解

同时，完整源代码已开源：**https://github.com/Egbert-Liu/TCommander**

---

## 4. TRAE 实践过程

TCommander 从 v0.1.0 到 v0.8.0 的完整开发过程均在 TRAE IDE 中完成。以下是关键开发阶段和 TRAE 实践记录：

### 阶段一：SSH 远程终端集成（6 阶段 13 任务）

**开发目标：** 在已有本地终端基础上，新增 SSH 远程终端能力，实现统一的终端后端抽象。

**TRAE 实践：**
1. 使用 TRAE 的 Auto 模式，让 AI 帮助设计 `TerminalBackend` 抽象接口，定义 `onData/onExit/write/resize/kill` 统一契约
2. 通过对话引导 AI 逐步实现 `SshBackend`（基于 ssh2 库），包括密码认证、私钥认证、keyboard-interactive 认证三种方式
3. 让 AI 实现 `safeStorage` 加密封装，确保 SSH 凭证安全存储（Windows DPAPI / macOS Keychain）
4. 引导 AI 完成渲染层适配：类型定义、配置表单切换、sessionActions 分流、连接状态推送、认证弹窗

**关键成果：** 6 阶段 13 任务全部完成，+2209 行代码，21 个文件变更，无破坏性变更。

> **Session ID：** `6a238aaf7e4e4829c3bbb7d7`（SSH 后端抽象 + 实现阶段）

### 阶段二：终端体验优化与卡片预览重构

**开发目标：** 解决卡片预览延迟、显示不全、状态抖动等问题。

**TRAE 实践：**
1. 首先让 AI 分析当前终端预览区的问题——字体过小（12px vs 全屏 13px）、横向滚动不符合习惯、行高过大
2. 让 AI 调研业界方案，生成了 `preview-options.html` 和 `preview-v2.html` 对比文件，参考 Wave Terminal、GitHub Actions、Docker Desktop 等产品的预览设计
3. 尝试方案 A（mini xterm 预览）+ 方案 C（终端实例池），构建 EXE 后发现体验反而变差，通过 TRAE 对话快速回滚到优化前状态
4. 重新迭代：最终采用「卡片即终端」方案——卡片直接挂载 xterm 实例，双击进入全屏仅做 DOM 转移

**关键决策（被拒绝的方案，AI 从中学习）：**
- ✗ Mini xterm 实例做卡片预览 → 卡片宽度变窄，显示性能差
- ✗ 终端实例池用于全屏模式 → 内容显示延迟
- ✗ 200ms 节流预览更新 → 渲染过慢
- ✓ 卡片即终端 + DOM 转移 → 零延迟、零重建、零 CPU 休眠

> **Session ID：** `6a238aaf7e4e4829c3bbb7d7`（终端优化迭代阶段，含回滚与重构）

### 阶段三：终端实例池架构与防抖排序

**开发目标：** 实现高性能的终端实例管理和会话排序。

**TRAE 实践：**
1. 让 AI 设计 `TerminalPool` 架构：一会话一 xterm 实例 + containerDiv 独立管理 + 统一订阅 onSessionOutput
2. 引导 AI 处理异步终端创建的竞态问题——使用 `pendingData` 缓存 PTY 输出，设置 256KB 上限防 OOM，用 `destroyed` Set 防止异步残留
3. 解决排序抖动问题：让 AI 实现基于 `stableActivityAt` 的防抖排序——仅在状态转换时更新排序位置，持续输出的 running 会话不会互相换位
4. 让 AI 自查代码问题并修复：rAF 回调访问已销毁实例、syncResize 在 detach 后发送 resize、重复 DOM 操作等

**关键成果：** `terminalPool.ts`、`sessionSort.ts`、`presetSort.ts` 核心模块完成，TypeScript 编译通过，生成 `TCommander Setup 0.8.0.exe`。

> **Session ID：** `6a238aaf7e4e4829c3bbb7d7`（实例池 + 防抖排序 + 自查修复）

### 阶段四：Hook 生态集成与参赛演示

**开发目标：** 构建 MCP Server + HTTP Hook API + CLI 工具，让 AI 助手能主动推送状态更新。

**TRAE 实践：**
1. 让 AI 设计 Hook HTTP Server（`hookServer.ts`），支持 `POST /api/session/:id/status` 和 `GET /api/sessions` 接口
2. 引导 AI 实现 MCP Server（`mcp-server.js`），注册 `tcommander_update_status`、`tcommander_list_sessions`、`tcommander_get_session` 三个工具
3. 让 AI 编写 Codex Wrapper 脚本（`tcommander-codex-wrapper.sh`），自动检测输出中的错误/确认/输入提示并更新状态
4. 让 AI 生成参赛演示 HTML 文件，包含完整界面模拟和 4 步演示工作流

> **Session ID：** `6a34a0939c37e7f13a1d90d6`（Hook 生态 + 演示页面生成）

---

### 开发心得

**1. TRAE 的"试错-回滚"工作流极高效**

在阶段二，我先让 AI 实现了 mini xterm 预览方案，构建后发现体验变差。通过 TRAE 对话，一条"回滚所有修改"的指令就恢复到优化前状态。这种快速试错的能力，让我敢于尝试激进方案，不用担心不可逆。

**2. 让 AI 自查代码质量**

阶段三结束后，我让 AI "自查系统问题并提出解决方案"。AI 主动发现了 6 个潜在问题：rAF 回调访问已销毁实例、pendingData 无大小限制导致 OOM 风险、attach 去重检查缺失等。这些是我在 review 时容易遗漏的边界情况。

**3. 被拒绝的方案会沉淀为项目记忆**

TRAE 的 memory 机制记录了被拒绝的方案（mini xterm 预览、200ms 节流等），后续对话中 AI 会主动避免重复这些已被否定的方向，保证了迭代方向的一致性。

**4. 多文件协同修改的上下文保持**

SSH 集成涉及 21 个文件的协同修改（后端抽象、安全存储、IPC 通道、渲染层类型、配置表单、状态推送等）。TRAE 在长对话中保持了完整的上下文理解，能准确处理文件间的依赖关系，不会遗漏接口适配。

---

### 项目数据

| 指标 | 数据 |
|------|------|
| 开发周期 | 2026.06 - 2026.07 |
| 版本 | v0.1.0 → v0.8.0 |
| 代码行数 | 9000+ 行 TypeScript |
| Git 提交 | 40+ commits |
| 功能分支 | feature/ssh、feature/card-as-live-terminal |
| 核心模块 | terminalPool、hookServer、MCP Server、SshBackend |

### GitHub 仓库

**https://github.com/Egbert-Liu/TCommander**
