# 验证清单

## 功能验证

### 卡片终端化
- [ ] 卡片预览区显示实时终端内容（xterm 渲染，非 HTML）
- [ ] 卡片内可直接输入命令并执行
- [ ] 终端颜色正确显示（ANSI 颜色与全屏一致）
- [ ] 卡片预览区横向不溢出（xterm 自适应宽度）
- [ ] 双击卡片进入全屏，无闪烁、内容连续
- [ ] 退出全屏后卡片恢复显示，内容连续
- [ ] 卡片宽度不小于 500px（CSS Grid minmax）

### 全屏终端
- [ ] 全屏终端从池获取实例，不重建
- [ ] Ctrl+Alt+左/右切换终端，无闪烁
- [ ] 全屏终端可正常输入
- [ ] 智能滚动（用户翻看历史时不被拉回底部）
- [ ] 主题切换后全屏终端颜色更新

### 全屏底部列表
- [ ] 底部列表按状态优先级 + stableActivityAt 排序
- [ ] 有状态会话排最前
- [ ] hover 显示 Tooltip 预览（尾部 6 行，带颜色）
- [ ] Tooltip 有状态色左边框
- [ ] 横向滚动正常
- [ ] 胶囊样式（高度 28px，字体 12px）

### 卡片抖动
- [ ] 两个持续输出的 running 会话不互相换位
- [ ] 状态转换（running→error）时卡片移动到正确位置
- [ ] 状态清除（error→running）时卡片回到无状态组
- [ ] running→idle 时卡片沉底

### 状态检测
- [ ] 规则匹配正常工作（error/needs-confirm/needs-input）
- [ ] 状态显示正确（卡片状态标签 + 颜色）
- [ ] 空闲检测（10s 无输出 → idle）正常

### 实例池生命周期
- [ ] 创建会话时实例正确创建
- [ ] 删除会话时实例正确销毁（无内存泄漏）
- [ ] 不可见卡片 detach 后不消耗 CPU
- [ ] 重新可见时 reattach 内容最新

## 性能验证
- [ ] 10 个会话同时输出，CPU 占用可控
- [ ] 20 个会话压力测试，无明显卡顿
- [ ] 长时间运行无内存泄漏

## 样式验证
- [ ] 卡片圆角 12px
- [ ] 有状态卡片左边框 3px 状态色
- [ ] 卡片阴影统一
- [ ] 全屏顶栏/底栏高度 48px
- [ ] 空状态样式优化

## 构建验证
- [ ] `npm run build` 无 TypeScript 错误
- [ ] `npm run dist:win` 生成 EXE
- [ ] EXE 可正常安装运行
